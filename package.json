{
  "name": "raznos-kanji",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "start": "vite --host 0.0.0.0 --port 3000",
    "preview": "vite preview",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "lint": "node --check src/core/*.js && node --check src/ui/*.js && node --check src/content/*.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
