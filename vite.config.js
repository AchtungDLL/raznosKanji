/**
 * Конфигурация нужна ровно для одного: чтобы облачные площадки
 * (AI Studio и подобные) видели рабочий dev-сервер на порту 3000
 * и не пытались «починить» проект, переписав его на свой стек.
 *
 * Сборки здесь нет и не нужно: исходники — обычные модули ES,
 * Vite отдаёт их как есть. Никакого React, TypeScript и Tailwind
 * в проекте не будет.
 */
export default {
  root: ".",
  server: { host: "0.0.0.0", port: 3000, strictPort: true },
  preview: { host: "0.0.0.0", port: 3000 },
  build: { outDir: "dist", target: "esnext" },
};
