/**
 * ПАРАМЕТРЫ БАЛАНСА. Единственное место, где они живут.
 *
 * Правило для исполнителей: числовая константа, влияющая на обучение или
 * нагрузку, лежит здесь и только здесь. Число, вписанное прямо в код модуля,
 * считается дефектом — его нельзя будет подобрать, не перечитывая весь проект.
 *
 * Значения взяты из расчёта в docs/balance-report.md. Они не окончательные:
 * их подбирают по живым данным мини-группы. Именно поэтому они собраны вместе.
 */

export const Balance = Object.freeze({
  // --- приток нового ----------------------------------------------------
  /**
   * Ползунок новых единиц в день. Середина 8 не случайна: при восьми
   * 1497 слов колоды закрываются за 6,2 месяца — это заявленная цель
   * «уровень за полгода» — при пиковой нагрузке около 18 минут в день.
   */
  NEW_PER_DAY_MIN: 3,
  NEW_PER_DAY_DEFAULT: 8,
  NEW_PER_DAY_MAX: 20,

  /** Сколько ответов за один заход по умолчанию. Ограничивает длину сессии. */
  BATTLE_LENGTH_DEFAULT: 30,

  // --- система повторений ------------------------------------------------
  START_EASE: 2.5,
  MIN_EASE: 1.3,
  EASE_PENALTY: 0.2,
  /** Какая доля интервала остаётся после срыва. */
  LAPSE_MULTIPLIER: 0.3,
  MAX_INTERVAL_DAYS: 365,
  /**
   * Рост интервала обязан быть не меньше чем на день.
   * Без этого единица с низким ease залипает на интервале в сутки навсегда,
   * и очередь растёт бесконечно. Проверено на модели.
   */
  MIN_INTERVAL_GROWTH_DAYS: 1,

  // --- грани -------------------------------------------------------------
  /**
   * Сколько правильных ответов подряд по основной грани нужно, чтобы
   * открылись WRITING и USAGE. Решение заказчика: сначала слово осваивается
   * по основной стороне, только потом вокруг него нарастает остальное.
   */
  FACET_UNLOCK_THRESHOLD: 3,
  /**
   * Доля показов, отдаваемая открытым дополнительным граням. Остальное
   * достаётся основной. Интервал у единицы общий, поэтому это лишь выбор,
   * что показать, а не отдельное расписание.
   */
  SECONDARY_FACET_SHARE: 0.4,

  // --- выносливость героя ------------------------------------------------
  /**
   * Запас выносливости на заход. Условие конца боя из решения заказчика:
   * бой идёт, пока не кончится выносливость героя или желание игрока.
   *
   * ВНИМАНИЕ: расход пока не определён — открыт вопрос Q26 (что именно
   * тратит выносливость: каждый ответ, только ошибки, время). До ответа
   * поле в сессии заполняется этим значением и не убывает. Не изобретать
   * расход самостоятельно.
   */
  STAMINA_START: 100,

  // --- осада -------------------------------------------------------------
  /**
   * Размер первой волны. Берётся из резерва зрелых единиц вне срока —
   * в очереди на сегодня лёгкого материала почти нет, он приходит редко
   * именно потому, что выучен. Резерв при любом темпе исчисляется сотнями.
   */
  INFANTRY_SIZE: 18,
  /** Граница между офицерами и остальными, в днях интервала. */
  OFFICER_MAX_INTERVAL_DAYS: 14,
  /** Единица считается ветераном, если срывалась за последние столько дней. */
  VETERAN_WINDOW_DAYS: 21,
  /** Либо если её ease опустился ниже этого значения. */
  VETERAN_EASE_THRESHOLD: 2.1,
  /**
   * Признак ветерана обязан быть скользящим. При накопительном
   * («срывался когда-либо дважды») к середине игры вся армия становится
   * ветеранами навсегда и третья волна теряет смысл.
   */

  // --- день --------------------------------------------------------------
  /** Час смены суток. Решение заказчика: осада обновляется в 4 утра. */
  DAY_ROLLOVER_HOUR: 4,

  // --- прогноз для советника --------------------------------------------
  /** Средняя длительность одного ответа для пересчёта нагрузки в минуты. */
  SECONDS_PER_ANSWER: 6,
  /** На сколько дней вперёд советник показывает прогноз. */
  FORECAST_HORIZON_DAYS: 14,

  /**
   * Вероятность «не вспомнил» в зависимости от зрелости единицы.
   * Оценка для новичка в японском, те же ступени, что в tools/srs_load_sim.py.
   * Прогноз обязан считать по ним, иначе советник будет врать игроку.
   * Подлежит замене на измеренные значения, когда появятся данные мини-группы.
   */
  FAIL_RATE_YOUNG: 0.22,        // интервал меньше 7 дней
  FAIL_RATE_MIDDLE: 0.15,       // интервал меньше 21 дня
  FAIL_RATE_MATURE: 0.10,       // интервал от 21 дня
  FAIL_RATE_LAPSE_STEP: 0.02,   // надбавка за каждый прошлый срыв
  FAIL_RATE_LAPSE_CAP: 4,       // больше этого числа срывов не учитывается

  // --- защита от завала --------------------------------------------------
  /**
   * Если просроченных единиц больше этого числа, выдача новых на сегодня
   * прекращается. Три пропущенных дня увеличивают очередь в 2,3-3,5 раза
   * (см. docs/balance-report.md); вываливать новое поверх такого долга —
   * верный способ потерять игрока.
   */
  BACKLOG_PAUSE_THRESHOLD: 120,
  /**
   * Мягкая зона: начиная с этого долга приток новых урезается пропорционально,
   * а не обрубается разом.
   */
  BACKLOG_SOFT_THRESHOLD: 60,
});
